# OneStop PCB — CAM + Multi-Machine Sender (Scaffold)

This is a scaffold generated from the architecture spec. See `docs/` for the full SPEC and BACKLOG.

## Quick Start
```bash
python -m venv .venv && source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -e .
python -m onestop   # runs CLI skeleton
```

## Docs
- `docs/SPEC-1-OneStopPCB.md`
- `docs/BACKLOG.md`
- `docs/DEPLOYMENT.md`
- `docs/THIRD_PARTY_NOTICES.md`
